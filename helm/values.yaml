replicaCount: 2

image:
  repository: image-registry.openshift-image-registry.svc:5000/ecommerce/shipping-service
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  port: 8443

route:
  enabled: true
  host: "shipping-service-ecommerce.apps-crc.testing"
  tls:
    termination: reencrypt
    insecureEdgeTerminationPolicy: Redirect
  annotations: {}

env:
  SERVER_PORT: "8443"
  SERVER_SSL_ENABLED: "true"
  KEYCLOAK_ISSUER_URI: "https://keycloak-platform.apps-crc.testing/realms/master"

secret:
  name: "shipping-serviceSecret"
  create: false
  data: {}
  # data example:
  #   DB_HOST: "db"
  #   DB_PORT: "5432"
  #   DB_NAME: "shipping-service"
  #   DB_USER: "shipping-service"
  #   DB_PASSWORD: "change-me"

tls:
  enabled: true
  secretName: "shipping-service-tls"

truststore:
  enabled: false
  secretName: "platform-truststore"
  mountPath: "/etc/ssl/openshift"
  password: "changeit"

resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

readinessProbe:
  path: /actuator/health
  initialDelaySeconds: 10
  periodSeconds: 10

livenessProbe:
  path: /actuator/health
  initialDelaySeconds: 20
  periodSeconds: 20

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
